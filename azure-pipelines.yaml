name: myAgentPool
trigger:
- master

pool: 
   vmImage: ubuntu-latest
   name: MyAget

stages:
- stage: Build
  displayName: Deploy
  jobs:
  - deployment: Deploy_to_VM
    environment: 
      name: 'EvnP3'
      resourceType: VirtualMachine
    strategy:
      runOnce:
         deploy:   
            steps:
               - script: echo "Hello world"